apm-server:
  host: "0.0.0.0:8200"
  agent:
    config:
      elasticsearch: # For remote agent configuration
        hosts: ["${ELASTICSEARCH_HOSTS}"]
        username: "elastic"
        password: "${ELASTIC_PASSWORD}"

output.elasticsearch:
  hosts: ["${ELASTICSEARCH_HOSTS}"]
  username: "elastic"
  password: "${ELASTIC_PASSWORD}"

setup.kibana:
  host: "${KIBANA_HOST}"
  username: "elastic"
  password: "${ELASTIC_PASSWORD}"